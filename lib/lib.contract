' When we need a file, the return value is the final thing returned,
' just like a block. So when creating a library, we simply export a
' bare namespace.

namespace {
  three is 3.
  four is nonary { 4 }.
  two-more-than is unary { POST + 2 }.
}
